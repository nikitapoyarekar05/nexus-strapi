'use strict';

/**
 * neighbourly-footer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::neighbourly-footer.neighbourly-footer');
